<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>폼 전송 결과</title>
    <style>
      span {
        color: blue;
        font-weight: 700;
      }
    </style>
  </head>
  <body>
    <h1>폼 전송 결과</h1>
    <ul>
      <li>이름:<%=user.username%></li>
      <li>성별:<%=user.gender%></li>
      <li>
        생일:<%=user.birthYear%>년<%=user.birthMonth%>월<%=user.birthDay%>일
      </li>
      <li>취미:<%=user.hobby%></li>
      <%if(user.myColor){%>
      <li>좋아하는 색상:<%=user.myColor%></li>
      <%}%> <%if(user.myNumber){%>
      <li>취미:<%=user.myNumber%></li>
      <%}%>
    </ul>
  </body>
</html>
